import type { DoctorContractModel } from '@/types';

export const doctorContractModelLabels: Record<DoctorContractModel, string> = {
  scp: 'SCP',
  pj: 'Pessoa jurídica (PJ)',
  pf: 'Pessoa física (PF)',
};

export const doctorContractModelShortLabels: Record<DoctorContractModel, string> = {
  scp: 'SCP',
  pj: 'PJ',
  pf: 'PF',
};
